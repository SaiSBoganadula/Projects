#include<stdio.h>

int main()
{
    int a,b,sum;
    printf("enter 'a' :");
    scanf("%d",&a);
    printf("enter 'b' :");
    scanf("%d",&b);
    sum = a + b;
    printf("the sum of two numbers is %d",sum );
}